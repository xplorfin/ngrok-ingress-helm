apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "ngrok.name" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "ngrok.name" . }}
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "ngrok.name" . }}
    spec:
      containers:
      - name: {{ template "ngrok.name" . }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command: ["ngrok"]
        args: ["http", "nginx-service"]
        ports:
        - containerPort: {{ .Values.ports.container }}
        {{- if .Values.env }}
        env:
          {{- range $key, $val := .Values.env }}
          - name: {{ $key }}
            value: {{ $val }}
          {{- end}}
        {{- end }}
